# Virtual environments
venv/
.venv/

# Python cache
__pycache__/
*.pyc

# Saved browser sessions (contain auth cookies — do not commit)
bob_session.json
bbc_sounds_session.json


# Audio files (large, not needed in repo)
*.m4a

# Data directories (clean/segmented corpus — derived, stored locally only)
data/clean/
data/segmented/

# Misc files
FYP_Demo.odp
demo and improvement.txt
.coverage

# Documentation
docs/